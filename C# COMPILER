using System;

namespace HelloWorld
{
  class Program
  {
    static void Main(string[] args)
    {
		string name = "Vanguard";
      Console.WriteLine(name);

		int num = 21;
	  Console.WriteLine(num);

	  	double tutu = 2.1;
	  Console.WriteLine(tutu);

	  	char Tipsy = 'D';
	  Console.WriteLine(Tipsy);    

	   bool ball = true;
	  Console.WriteLine(ball);
    }
  }
}
===========================================================
using System;

namespace HelloWorld
{
  class Program
  {
    static void Main(string[] args)
    {
		string name = "Micko";
		int num = 19;
	  	double tutu = 19.99;
		char Tipsy = 'D';
		bool ball = false;

	  Console.WriteLine ("Hi my name is "+name+" ,I am "+num+" years old. I bought a apple for a price of "+tutu+" per kilo. The name of the store is "+Tipsy+" store, and it is "+ball+" for selling a high value fruits.");
    }
  }
}
